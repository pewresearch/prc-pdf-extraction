{
	"name": "prc/pdf-extraction",
	"description": "Extract text from PDF documents using OCR services, making data accessible to LLMs",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=8.2"
	},
	"require-dev": {
		"phpunit/phpunit": "^9.6",
		"yoast/phpunit-polyfills": "^1.0",
		"wp-phpunit/wp-phpunit": "^6.1"
	},
	"scripts": {
		"test": [
			"phpunit"
		],
		"test-ms": [
			"WP_MULTISITE=1 phpunit"
		],
		"test-vip": [
			"phpunit --configuration phpunit-vip.xml.dist"
		]
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	}
}
